import { Meta } from '@/layout/Meta';
import { Main } from '@/templates/Main';

const Create = () => {
  return (
    <Main meta={<Meta title="Profile" description="Description" />}>Hello</Main>
  );
};

export default Create;
